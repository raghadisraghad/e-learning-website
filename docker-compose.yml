version: '3'

services:
  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/code
    ports:
      - "8000:8000"
    environment:
      - DEBUG=1
      - DB_HOST=localhost
      - DB_NAME=db.sqlite3
      - DB_USER=
      - DB_PASSWORD=
